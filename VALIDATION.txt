IPAP BASE STUDY PROGRAM v2.0.0 — VALIDATION
================================================

Completed checks:
PASS — Original uploaded Heart question content preserved exactly: all 400 prompts, answer choices, correct-answer indices, and source-page values match the uploaded source after ignoring the newly appended permanent-ID field.
PASS — Exactly 400 permanent IDs are present.
PASS — Permanent IDs are unique and sequential HEART-0001 through HEART-0400.
PASS — Existing localStorage key heartQuizTrainerAdaptiveProgressV1 is preserved.
PASS — Legacy positional IDs migrate to permanent IDs in question performance records.
PASS — Legacy positional IDs migrate inside coverage history.
PASS — Legacy positional IDs migrate inside saved in-progress sessions and missed-question IDs.
PASS — Legacy bank identifier heart-400q-ipap-v1 remains accepted on import.
PASS — New bank-family identifier is ipap-adaptive-study-v1.
PASS — New progress payload schema is version 4.
PASS — New transfer codes use IPAP4.
PASS — Legacy IPAP3 transfer codes remain parsable/import-compatible.
PASS — Adaptive quiz construction operates on permanent IDs.
PASS — Main inline JavaScript passes Node syntax validation.
PASS — Service-worker JavaScript passes Node syntax validation.
PASS — manifest.webmanifest and version.json are valid JSON.
PASS — Service-worker update flow is guarded so automatic reload is reserved for an explicitly requested app update.

Hosted Safari/iPad behavior still requires final real-device validation after deployment because service workers and Add to Home Screen operate only in the hosted browser environment.
